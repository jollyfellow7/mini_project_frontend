name: chungsora_child_v2
description: 청소해라 자녀 앱 v2 — WebView + FCM + Lock
publish_to: none
version: 0.5.0+5

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  shared_preferences: ^2.3.3
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  webview_flutter: ^4.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
